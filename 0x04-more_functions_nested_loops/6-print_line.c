#include "main.h"

/**
 * print_line - prints straight line is printed.
 * @n: times staright line is printed.
 * Return: no return
 */
void print_lines(int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		_putchar(95);
	}
	_putchar('\n');
}
